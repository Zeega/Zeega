{
    {% if user is defined and user is not empty %}
    "id" : {{ user.id | json_encode | raw}},
    "display_name" : {{ user.displayName | json_encode | raw}},
    "thumbnail_url" : {{ user.thumbUrl | default("https://s3.amazonaws.com/zeegastatic/default_profile.jpeg") | json_encode | raw}}
    {% endif %}
}
