{
    {% if schedule is defined and schedule is not null %}
    "id" : {{ schedule.getId }},
    "query" : {{ schedule.getQuery | json_encode | raw}},
    "date_created" : "{{ schedule.getDateCreated | date("Y-m-d") }}",
    "date_updated" : "{{ schedule.getDateUpdated | date("Y-m-d") }}",
    "status" : {{ schedule.getStatus | json_encode | raw}},
    "enabled" : {{ schedule.enabled | json_encode | raw}}
    {% endif %}
}
