{
		"id" : {{ frame.getId }},
		"project_id" : {{frame.getProject.id | default([ ]) | json_encode | raw}},
		"attr" : {{frame.attr | default([ ]) | json_encode | raw}},
		"thumbnail_url" : {{ frame.getThumbnailUrl | default("") | json_encode | raw}},
		"layers":[
		{% for layer in frame.getLayers %}
			{% include 'ZeegaApiBundle:Layers:show.json.twig' with {'layer': layer} %}	
			{%- if loop.index0 < frame.getLayers|length - 1 -%}
			,
			{%- endif -%}
	    {% endfor %}
		]
}
