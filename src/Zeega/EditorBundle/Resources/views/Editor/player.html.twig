<!doctype html>
<html>
<head>
	<meta charset="utf-8">

	<title>
		
	</title>
	<meta name="description" content="">
	<meta name="author" content="">
	<link rel="stylesheet" href="{{ asset('gamma/css/zeega.css')}}">
		<script> 
			sessionStorage.setItem('directory', '{{directory}}');
			sessionStorage.setItem('hostname','{{hostname}}');
			sessionStorage.setItem('projectId','{{projectId}}');
		</script>
	
</head>

<body>




	<script src="{{ asset('gamma/js/jquery/ui/js/jquery-1.5.1.min.js')}}"></script>
	<script defer src="{{ asset('gamma/js/libraries/underscore.js')}}"></script>
	
	<script defer src="{{ asset('gamma/js/helpers/zeega.extends.js')}}"></script>
	<script defer src="{{ asset('gamma/js/helpers/zeega.helpers.js')}}"></script>

	<script defer src="{{ asset('gamma/js/layers/zeega._layer.js')}}"></script>
	<script defer src="{{ asset('gamma/js/layers/zeega.image.js')}}"></script>
	
	
	
	<script defer src="{{ asset('gamma/js/core/player.js')}}"></script>


	<script type="text/javascript" language="Javascript">
		$('.citation-tab').click(function(){
			
			$('#citation').animate({ height : '100px' })
			
			closeOpenCitationTabs();
			
			$(this).closest('li').find('.citation-content').fadeIn();
		})
		

		$('#citation').mouseleave(function(){
			closeCitationBar();
		})
		
		function closeCitationBar()
		{
			$('#citation').animate({ height : '20px' })
			//$('#hide-citation').fadeOut();
			closeOpenCitationTabs();
		}
		
		function closeOpenCitationTabs()
		{
			$('.citation-tab').closest('ul').children('li').each(function(i,el){
				if($(el).find('.citation-content').is(':visible')) $(el).find('.citation-content').hide();
			})	
		}
		$(document).ready(function(){
			$.get(sessionStorage.getItem('hostname')+sessionStorage.getItem('directory')+'projects/'+sessionStorage.getItem('projectId')+'/all',function(data){
				Player.init(data);
			});		
		});

	</script> 

</body>
</html>
